// Parcel machine page (kiosk + operator console) — built in M5. The route and
// dark kiosk shell exist now so the second ingress has something to serve.
export function MachinePage() {
  return (
    <div className="flex min-h-dvh items-center justify-center bg-neutral-950 text-neutral-400">
      <p className="text-xl">Pakketautomaat — volgt in M5</p>
    </div>
  );
}
